; ==============================================================================
; alarma.inc — Estado de alarma (config/activa/ninguna), comparación HH:MM
; contra RTC, notificación (parpadeo de atributo de pantalla + beep vía
; puertos 0x61/0x42/0x43), cancelación.
; ==============================================================================
